name: v36-exits-parallel

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v36-exits-parallel.yml'
  workflow_dispatch:

concurrency:
  group: v36-exits-parallel
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by frozen local full-grid rule result and V3.6 global holdout lock.'
