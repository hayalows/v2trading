name: v36-zero-base-quant

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v36-zero-base-quant.yml'
  workflow_dispatch:

concurrency:
  group: v36-zero-base-quant
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by completed ML matrix, local full rule grid, V3.6B and V3.6C under the global holdout lock.'
