name: v36b-factor-exits-parallel

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v36b-factor-exits-parallel.yml'
  workflow_dispatch:

concurrency:
  group: v36b-factor-exits-parallel
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by V3.6B global-holdout-locked factor workflow.'
