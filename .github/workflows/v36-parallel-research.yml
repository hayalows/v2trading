name: v36-parallel-research

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v36-parallel-research.yml'
  workflow_dispatch:

concurrency:
  group: v36-parallel-research
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V3.6 48-config ML matrix complete with zero pre-holdout survivors; workflow archived.'
