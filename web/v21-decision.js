(()=>{
  const URL='https://uykjgyqoptsvvkaifphm.supabase.co/functions/v1/decision-intelligence';
  let data=null,busy=false,lastSymbol=null;
  const safe=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const price=v=>Number.isFinite(Number(v))?Number(v).toFixed(5):'—';
  const pct=v=>Number.isFinite(Number(v))?`${Math.round(Number(v)*100)}%`:'—';
  const cap=v=>String(v??'').replaceAll('_',' ').toLowerCase().replace(/\b\w/g,m=>m.toUpperCase());
  const symbol=()=>{if(typeof selected!=='undefined'&&selected)return selected;const a=document.querySelector('.pairSwitch .active,[data-pair].active');const t=a?.textContent||'';return t.includes('GBP')?'GBPUSD':'EURUSD'};
  const pair=()=>data?.pairs?.find(x=>x.symbol===symbol())||null;
  const stageNames=['Quiet','Liquidity nearby','POI used','Sweep','Wait for BOS','BOS','Fresh POI','Approaching','Entry zone'];
  function trustTone(x){return x==='HIGH'?'good':x==='BLOCKED'?'block':'warn'}
  function modeTitle(p){const f=p.formation,t=p.activeTrade,m=p.decision?.mode;if(m==='DUAL_ATTENTION')return `${cap(t?.direction)} paper trade open · ${cap(f.direction)} formation developing`;if(m==='DATA_BLOCKED')return 'Market intelligence paused';if(m==='PAPER_OPEN')return `${cap(t?.direction)} paper trade is open`;if(m==='PLAN_ARMED')return `${cap(t?.direction)} paper plan is waiting`;if(m==='POI_READY')return `${cap(f.direction)} POI is ready`;if(m==='BOS_CONFIRMED')return `${cap(f.direction)} BOS confirmed`;if(m==='FORMATION_WATCH')return `${cap(f.direction)} formation is developing`;return 'No mature setup right now'}
  function modeCopy(p){const f=p.formation,t=p.activeTrade;if(p.decision?.mode==='DUAL_ATTENTION')return `These are separate market objects. The ${String(t?.direction).toUpperCase()} paper trade keeps its original frozen geometry while the new ${String(f.direction).toUpperCase()} formation must independently earn BOS and a fresh POI.`;if(p.decision?.mode==='DATA_BLOCKED')return p.decision?.dataTrust?.reason||'The feed failed a quality gate.';if(t?.status==='open')return 'The research entry already occurred. The engine is tracking the frozen stop and target while new formations are evaluated separately.';if(t?.status==='armed')return 'The structural setup is frozen, but no entry is counted until the baseline midpoint rule is satisfied.';return f?.label||'The detector is observing completed structure.'}
  function stageRail(stage){return `<div class="v21StageRail" aria-label="Formation stage ${stage} of 8">${Array.from({length:9},(_,i)=>`<i class="v21Stage ${i<=stage?'on':''} ${i===stage?'current':''}"></i>`).join('')}</div><div class="v21StageCaption"><span>Stage ${stage}/8 · ${safe(stageNames[stage]||'Unknown')}</span><span>Structural progress, not win probability</span></div>`}
  function formationCard(p){const f=p.formation,d=f.direction||'none';return `<article class="v21Card ${p.decision?.mode==='DUAL_ATTENTION'?'v21Dual':''}"><div class="v21Track"><div class="v21TrackIcon">◎</div><div><div class="v21Label">Market formation now</div><strong>${safe(p.symbol)} · Stage ${f.stage}/8 · ${safe(cap(f.code))}</strong><small>${safe(f.label)}${f.regime?` · ${safe(cap(f.regime))} regime`:''}${f.context?` · ${safe(cap(f.context))} HTF context`:''}</small></div>${d!=='none'?`<span class="v21Direction ${safe(d)}">${safe(d)}</span>`:''}</div><div class="v21Next"><b>Next structural checkpoint</b><p>${safe(p.decision?.formationNext||'Wait for the next completed structural event.')}</p></div></article>`}
  function tradeCard(p){const t=p.activeTrade;if(!t)return `<article class="v21Card"><div class="v21Track"><div class="v21TrackIcon">◇</div><div><div class="v21Label">Existing paper plan</div><strong>None active</strong><small>New market formations can develop without being confused with an old trade.</small></div></div></article>`;const pen=Number(t.max_poi_penetration);return `<article class="v21Card ${p.decision?.mode==='DUAL_ATTENTION'?'v21Dual':''}"><div class="v21Track"><div class="v21TrackIcon">◇</div><div><div class="v21Label">Existing paper ${safe(t.status)}</div><strong>${safe(p.symbol)} · ${safe(cap(t.direction))} · ${safe(cap(t.poi_lifecycle_state||t.status))}</strong><small>${t.status==='open'?'Entry recorded; SL/TP tracking is active.':'Frozen plan is waiting for the baseline entry.'}${Number.isFinite(pen)?` · Max POI penetration ${pct(pen)}`:''}</small></div><span class="v21Direction ${safe(t.direction)}">${safe(t.direction)}</span></div><div class="v21PoiLevels"><div><span>Entry</span><b>${price(t.entry_price)}</b></div><div><span>Stop</span><b>${price(t.stop_price)}</b></div><div><span>Target</span><b>${price(t.target_price)}</b></div><div><span>POI low</span><b>${price(t.poi_low)}</b></div><div><span>POI high</span><b>${price(t.poi_high)}</b></div><div><span>Risk / ATR</span><b>${Number.isFinite(Number(t.risk_atr))?Number(t.risk_atr).toFixed(2):'—'}</b></div></div>${p.decision?.tradeNext?`<div class="v21Next"><b>Trade track</b><p>${safe(p.decision.tradeNext)}</p></div>`:''}</article>`}
  function intelligenceCard(p){const r=p.decision?.router||{},a=p.decision?.shadowAgreement||{},trust=p.decision?.dataTrust||{},n=Number(r.evidenceN||0),gate=Math.min(100,n/30*100),route=r.route==='ABSTAIN'?'ABSTAIN':cap(r.route),drift=p.learning?.drift||{},ds=drift.stateTwin||{},dt=drift.ttm||{};return `<article class="v21Card"><div class="v21Label">Decision intelligence</div><h3>${safe(route)} · evidence-gated model routing</h3><p>${safe(r.reason||'The model router is building prospective evidence.')}</p><div class="v21Gate"><div class="v21GateTop"><span>Independent prospective model outcomes</span><b>${n} / 30 minimum</b></div><div class="v21Bar"><i style="width:${gate}%"></i></div></div><div class="v21ModelStatus"><b>Hidden observer agreement: ${safe(a.status||'UNAVAILABLE')}</b><span>${safe(a.note||'Raw probabilities are not shown.')}</span></div><div class="v21Metrics" style="margin-top:10px"><div class="v21Metric"><span>StateTwin drift</span><b>${safe(ds.status||'BUILDING')}</b><small>${Number(ds.n||0)} scored outcomes</small></div><div class="v21Metric"><span>TTM drift</span><b>${safe(dt.status||'BUILDING')}</b><small>${Number(dt.n||0)} scored outcomes</small></div></div>${p.decision?.sourceMismatch?`<div class="v21LearningNote"><strong>Source guard:</strong> ${safe(p.decision.sourcePolicy)}</div>`:''}<div class="v21LearningNote">The router predicts structural progression only. It cannot change the 50% paper-entry rule, reveal a trade win probability, or override weak data.</div></article>`}
  function learningCard(p){const d=p.learning?.depth||{},depths=d.depths||[],m=depths.find(x=>Number(x.depthPct)===50),max=Math.max(0,...depths.map(x=>Number(x.scored||0)));return `<article class="v21Card"><div class="v21Label">Continuous learning</div><h3>Learn first. Promote only after evidence.</h3><p>The engine stores prospective formation outcomes and a 0–100% POI depth shadow. Backfilled observations never earn promotion.</p><div class="v21Metrics" style="margin-top:12px"><div class="v21Metric"><span>Depth shadow rows</span><b>${Number(d.prospectiveRows||0)}</b><small>21 depths per valid plan</small></div><div class="v21Metric"><span>50% depth scored</span><b>${Number(m?.scored||0)}</b><small>performance hidden until 30</small></div><div class="v21Metric"><span>Most scored depth</span><b>${max}</b><small>100 required for stronger evidence</small></div><div class="v21Metric"><span>Baseline</span><b>50%</b><small>unchanged</small></div></div></article>`}
  function render(){const p=pair(),root=document.getElementById('v21DecisionBoard');if(!root||!p)return;const f=p.formation,trust=p.decision?.dataTrust||{},stage=Math.max(0,Math.min(8,Number(f.stage||0)));root.innerHTML=`<section class="v21Hero"><div class="v21Top"><div><div class="v21Eyebrow">${safe(p.symbol)} · Decision Intelligence</div><h2>${safe(modeTitle(p))}</h2><div class="v21HeroCopy">${safe(modeCopy(p))}</div><div class="v21Pills"><span class="v21Pill ${trustTone(trust.level)}">Data ${safe(trust.level||'—')} · ${safe(trust.label||'Unknown')}</span><span class="v21Pill">${safe(cap(f.regime||'unknown'))} regime</span><span class="v21Pill">Model ${safe(p.decision?.router?.route||'ABSTAIN')}</span>${p.decision?.mode==='DUAL_ATTENTION'?'<span class="v21Pill warn">Two independent tracks</span>':''}</div></div><div class="v21Price"><b>${price(p.referencePrice)}</b><span>${safe(p.symbol)} reference</span></div></div>${stageRail(stage)}<div class="v21Actions"><button class="v21Action primary" onclick="setView('chartView')">Open chart</button><button class="v21Action" onclick="setView('tradesView')">Paper trades</button><button class="v21Action" onclick="setView('evidenceView')">Research</button></div></section><div class="v21Grid"><div class="v21TrackStack">${formationCard(p)}${tradeCard(p)}</div><div class="v21TrackStack">${intelligenceCard(p)}${learningCard(p)}</div></div>`;const legacy=document.getElementById('focusBoard');if(legacy)legacy.classList.add('v21HiddenLegacy');updateResearch(p)}
  function updateResearch(p){let card=document.getElementById('v21LearningResearch');const view=document.getElementById('evidenceView');if(!view)return;if(!card){card=document.createElement('article');card.id='v21LearningResearch';card.className='card v21ResearchCard';const anchor=document.getElementById('poiDepthLearning')||document.getElementById('shadowArenaResearch')||view.querySelector('.stats');anchor?.insertAdjacentElement('afterend',card)}if(!card)return;const r=p.decision?.router||{},d=p.learning?.depth||{},dr=p.learning?.drift||{};card.innerHTML=`<div class="cardLabel"><span class="material-symbols-rounded">neurology</span>V2.1 learning policy</div><h3>Models can earn influence. They cannot inherit it.</h3><p>V2 routes hidden structural observers only after paired prospective evidence beats the frozen base. Source mismatch, drift or weak samples keep the engine in ABSTAIN.</p><div class="v21ResearchRows"><div><span>Router evidence</span><b>${Number(r.evidenceN||0)} / 30</b></div><div><span>StateTwin drift</span><b>${safe(dr.stateTwin?.status||'BUILDING')}</b></div><div><span>TTM drift</span><b>${safe(dr.ttm?.status||'BUILDING')}</b></div><div><span>POI shadow rows</span><b>${Number(d.prospectiveRows||0)}</b></div><div><span>Strong evidence gate</span><b>100+</b></div><div><span>Entry baseline</span><b>50% frozen</b></div></div><div class="paperNotice"><strong>Research boundary</strong>Model output concerns structural progression, not broker profitability. No raw model probability is exposed to Focus and no model can automatically change the paper-entry strategy.</div>`}
  function mount(){let root=document.getElementById('v21DecisionBoard');if(!root){root=document.createElement('div');root.id='v21DecisionBoard';root.className='v21Board';const legacy=document.getElementById('focusBoard');legacy?.insertAdjacentElement('beforebegin',root)}const intro=document.querySelector('.intro');if(intro){const e=intro.querySelector('.eyebrow'),h=intro.querySelector('h1'),p=intro.querySelector('p');if(e)e.textContent='Version 2.1 · Decision Intelligence';if(h)h.textContent='See the market. See the uncertainty.';if(p)p.textContent='Track formations, frozen paper plans, source quality and model learning as separate layers. The engine abstains when evidence is weak.'}render()}
  async function load(){if(busy)return;busy=true;try{const r=await fetch(URL,{cache:'no-store'});if(!r.ok)throw new Error(`Decision Intelligence ${r.status}`);data=await r.json();mount();render()}catch(e){console.warn('V2.1 Decision Intelligence',e)}finally{busy=false}}
  document.addEventListener('click',e=>{if(e.target.closest('.pairSwitch,[data-pair]'))setTimeout(()=>{const s=symbol();if(s!==lastSymbol){lastSymbol=s;render()}},80)});
  document.getElementById('refresh')?.addEventListener('click',()=>setTimeout(load,250));
  mount();load();setInterval(()=>{const s=symbol();if(s!==lastSymbol){lastSymbol=s;render()}},1000);setInterval(load,60_000);
})();