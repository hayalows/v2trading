import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const SB=Deno.env.get('SUPABASE_URL')!,KEY=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const db=createClient(SB,KEY,{auth:{persistSession:false}});const H={'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'};
Deno.serve(async req=>{if(req.method!=='GET')return new Response(JSON.stringify({error:'GET only'}),{status:405,headers:H});try{const q=await db.from('discord_alert_state').select('snapshot').eq('state_key','pulse:v25:macro').maybeSingle();if(q.error)throw Error(q.error.message);const s:any=q.data?.snapshot??null;if(s){let changed=false;if(s.m15===true){if(s.h1!==true){s.h1=true;changed=true}if(s.h4!==true){s.h4=true;changed=true}}else if(s.h1===true&&s.h4!==true){s.h4=true;changed=true}if(changed){const u=await db.from('discord_alert_state').update({snapshot:s,updated_at:new Date().toISOString()}).eq('state_key','pulse:v25:macro');if(u.error)throw Error(u.error.message)}}const r=await fetch(`${SB}/functions/v1/discord-v25`,{headers:{'Cache-Control':'no-cache'},signal:AbortSignal.timeout(55000)}),t=await r.text();return new Response(t,{status:r.status,headers:H})}catch(e){console.error(e);return new Response(JSON.stringify({error:e instanceof Error?e.message:String(e)}),{status:500,headers:H})}});
