import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const db = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!, { auth: { persistSession: false } });
const CORS = {"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, POST, OPTIONS","Cache-Control":"no-store"};
const INSTRUMENTS:Record<string,{name:string;chart:string;proxy:boolean}>={EURUSD:{name:"Euro / U.S. Dollar",chart:"EURUSD=X",proxy:false},GBPUSD:{name:"British Pound / U.S. Dollar",chart:"GBPUSD=X",proxy:false},XAUUSD:{name:"Gold / U.S. Dollar",chart:"GC=F",proxy:true},US30:{name:"Dow / US30",chart:"YM=F",proxy:true}};
type Bar={ts:string;open:number;high:number;low:number;close:number;volume:number|null};
const TF_SECONDS:Record<string,number>={"15m":900,"1h":3600,"1d":86400};

function response(data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:{...CORS,"Content-Type":"application/json; charset=utf-8"}})}
function finite(x:unknown):number|null{const n=Number(x);return Number.isFinite(n)?n:null}
function clamp(v:number,lo:number,hi:number){return Math.max(lo,Math.min(hi,v))}
function median(values:number[]){if(!values.length)return NaN;const x=[...values].sort((a,b)=>a-b),m=Math.floor(x.length/2);return x.length%2?x[m]:(x[m-1]+x[m])/2}
function ema(values:number[],period:number){if(!values.length)return[];const k=2/(period+1),out=[values[0]];for(let i=1;i<values.length;i++)out.push(values[i]*k+out[i-1]*(1-k));return out}
function tr(bars:Bar[]){return bars.map((b,i)=>i===0?b.high-b.low:Math.max(b.high-b.low,Math.abs(b.high-bars[i-1].close),Math.abs(b.low-bars[i-1].close)))}
function rollingMean(values:number[],n:number){const out:number[]=[];let sum=0;for(let i=0;i<values.length;i++){sum+=values[i];if(i>=n)sum-=values[i-n];out.push(sum/Math.min(i+1,n))}return out}
function cleanCompleted(bars:Bar[],seconds:number){let out=bars.filter(b=>b.open>0&&b.high>0&&b.low>0&&b.close>0&&b.high>=b.low);while(out.length>1){const t=new Date(out.at(-1)!.ts).getTime();if(Date.now()-t<seconds*1000*.9)out=out.slice(0,-1);else break}return out}
function aggregate(bars:Bar[],hours:number){const ms=hours*3600_000,groups=new Map<number,Bar[]>();for(const b of bars){const key=Math.floor(new Date(b.ts).getTime()/ms)*ms,g=groups.get(key)??[];g.push(b);groups.set(key,g)}return[...groups.entries()].sort((a,b)=>a[0]-b[0]).map(([t,g])=>({ts:new Date(t).toISOString(),open:g[0].open,high:Math.max(...g.map(x=>x.high)),low:Math.min(...g.map(x=>x.low)),close:g.at(-1)!.close,volume:g.some(x=>x.volume!=null)?g.reduce((s,x)=>s+(x.volume??0),0):null}))}

async function yahoo(symbol:string,interval:string,range:string){const encoded=encodeURIComponent(symbol);let last="";for(const host of["query1.finance.yahoo.com","query2.finance.yahoo.com"]){try{const url=`https://${host}/v8/finance/chart/${encoded}?interval=${interval}&range=${range}&includePrePost=false&events=div%2Csplits`,r=await fetch(url,{headers:{"User-Agent":"Mozilla/5.0 V2QuantResearch/3.0","Accept":"application/json"}});if(!r.ok){last=`${host}:${r.status}`;continue}const j=await r.json(),root=j?.chart?.result?.[0];if(!root?.timestamp?.length){last=`${host}:empty`;continue}const q=root.indicators?.quote?.[0]??{},bars:Bar[]=[];for(let i=0;i<root.timestamp.length;i++){const o=finite(q.open?.[i]),h=finite(q.high?.[i]),l=finite(q.low?.[i]),c=finite(q.close?.[i]);if(o==null||h==null||l==null||c==null||o<=0||h<=0||l<=0||c<=0||h<l)continue;bars.push({ts:new Date(root.timestamp[i]*1000).toISOString(),open:o,high:h,low:l,close:c,volume:finite(q.volume?.[i])})}return{bars,meta:root.meta??{},source:"Yahoo Finance public chart"}}catch(e){last=String(e)}}throw new Error(`chart fetch failed ${last}`)}
async function cached(key:string,ttlMs:number,loader:()=>Promise<any>){const read=await db.from("provider_cache").select("payload,fetched_at,expires_at,status").eq("cache_key",key).maybeSingle(),old=read.data;if(old&&new Date(old.expires_at).getTime()>Date.now())return{payload:old.payload,fetchedAt:old.fetched_at,status:old.status};try{const payload=await loader(),now=new Date(),expires=new Date(now.getTime()+ttlMs),w=await db.from("provider_cache").upsert({cache_key:key,payload,fetched_at:now.toISOString(),expires_at:expires.toISOString(),status:"ok",error:null});if(w.error)throw new Error(w.error.message);return{payload,fetchedAt:now.toISOString(),status:"ok"}}catch(e){if(old)return{payload:old.payload,fetchedAt:old.fetched_at,status:"stale",error:String(e)};return{payload:null,fetchedAt:new Date(0).toISOString(),status:"error",error:String(e)}}}
async function fxReference(){return cached("exchangerate-dev-usd",90_000,async()=>{const r=await fetch("https://api.exchangerate.dev/v1/latest/USD?symbols=EUR,GBP",{headers:{"Accept":"application/json","User-Agent":"V2QuantResearchLab/3.0"}});if(!r.ok)throw new Error(`exchangerate.dev ${r.status}`);const j=await r.json();if(j?.result!=="success"||!j?.rates?.EUR||!j?.rates?.GBP)throw new Error("invalid FX reference response");return j})}
async function goldReference(){return cached("goldprice-xau",90_000,async()=>{const r=await fetch("https://api.goldprice.dev/v1/prices?symbol=XAU-USD-SPOT",{headers:{"Accept":"application/json","User-Agent":"V2QuantResearchLab/3.0"}});if(!r.ok)throw new Error(`goldprice.dev ${r.status}`);const j=await r.json();if(j?.error)throw new Error(String(j.error));return j})}

function trend(bars:Bar[]){if(bars.length<55)return{label:"insufficient",strength:0,score:0,ema20:null,ema50:null};const closes=bars.map(x=>x.close),e20=ema(closes,20),e50=ema(closes,50),atr=rollingMean(tr(bars),14),i=bars.length-1,a=Math.max(atr[i],Math.abs(closes[i])*1e-5),p1=clamp((closes[i]-e20[i])/a,-3,3),p2=clamp((e20[i]-e50[i])/a,-3,3),p3=clamp((e20[i]-e20[Math.max(0,i-5)])/a,-3,3),score=p1*.55+p2*.75+p3*.45,label=score>.35?"bullish":score<-.35?"bearish":"mixed";return{label,strength:Math.round(clamp(Math.abs(score)*38,0,100)),score,ema20:e20[i],ema50:e50[i]}}
function swings(bars:Bar[]){const highs:any[]=[],lows:any[]=[];for(let i=2;i<bars.length-2;i++){if(bars[i].high>bars[i-1].high&&bars[i].high>=bars[i-2].high&&bars[i].high>bars[i+1].high&&bars[i].high>=bars[i+2].high)highs.push({i,price:bars[i].high,ts:bars[i].ts});if(bars[i].low<bars[i-1].low&&bars[i].low<=bars[i-2].low&&bars[i].low<bars[i+1].low&&bars[i].low<=bars[i+2].low)lows.push({i,price:bars[i].low,ts:bars[i].ts})}return{lastHigh:highs.at(-1)??null,lastLow:lows.at(-1)??null}}
function session(){const h=new Date().getUTCHours(),l=h>=7&&h<16,n=h>=13&&h<22,a=h>=0&&h<9;if(l&&n)return"London / New York overlap";if(l)return"London";if(n)return"New York";if(a)return"Asia";return"Transition / off-hours"}
function formation(bars:Bar[],structurePrice:number){if(bars.length<45)return{stage:0,code:"NO_SETUP",label:"Not enough M15 history",direction:null,maturity:0,poiHigh:null,poiLow:null,distancePoiAtr:null,details:{}};const atr=rollingMean(tr(bars),14),n=bars.length,a=Math.max(atr.at(-1)!,Math.abs(bars.at(-1)!.close)*1e-5);let sweep:any=null;for(let i=Math.max(22,n-12);i<n;i++){const prior=bars.slice(i-20,i),ph=Math.max(...prior.map(x=>x.high)),pl=Math.min(...prior.map(x=>x.low)),ai=Math.max(atr[i],a),bear=bars[i].high>ph+.03*ai&&bars[i].close<ph,bull=bars[i].low<pl-.03*ai&&bars[i].close>pl;if(bear||bull)sweep={i,direction:bear?"short":"long",ts:bars[i].ts}}const sw=swings(bars.slice(-60));if(!sweep){const hi=sw.lastHigh?.price??Math.max(...bars.slice(-20).map(x=>x.high)),lo=sw.lastLow?.price??Math.min(...bars.slice(-20).map(x=>x.low)),dh=Math.abs(structurePrice-hi)/a,dl=Math.abs(structurePrice-lo)/a;if(Math.min(dh,dl)<=.35){const dir=dl<dh?"long":"short";return{stage:1,code:"LIQUIDITY_NEARBY",label:`Structure price is near ${dl<dh?"sell-side":"buy-side"} liquidity`,direction:dir,maturity:30,poiHigh:null,poiLow:null,distancePoiAtr:null,details:{distanceHighAtr:dh,distanceLowAtr:dl}}}return{stage:0,code:"NO_SETUP",label:"No active V2 formation",direction:null,maturity:10,poiHigh:null,poiLow:null,distancePoiAtr:null,details:{distanceHighAtr:dh,distanceLowAtr:dl}}}const pre=bars.slice(Math.max(0,sweep.i-8),sweep.i),bosHigh=Math.max(...pre.map(x=>x.high)),bosLow=Math.min(...pre.map(x=>x.low));let bos=-1;for(let i=sweep.i+1;i<n;i++)if((sweep.direction==="long"&&bars[i].close>bosHigh)||(sweep.direction==="short"&&bars[i].close<bosLow)){bos=i;break}if(bos<0){const age=n-1-sweep.i;return{stage:age>=1?4:3,code:age>=1?"WAITING_FOR_BOS":"SWEEP_CONFIRMED",label:age>=1?"Liquidity sweep confirmed; waiting for BOS":"Liquidity sweep confirmed",direction:sweep.direction,maturity:age>=1?50:42,poiHigh:null,poiLow:null,distancePoiAtr:null,details:{sweepTime:sweep.ts,bosReference:sweep.direction==="long"?bosHigh:bosLow}}}let poi=-1;for(let i=bos;i>=sweep.i;i--){const opposite=sweep.direction==="long"?bars[i].close<bars[i].open:bars[i].close>bars[i].open;if(opposite){poi=i;break}}if(poi<0)return{stage:5,code:"BOS_CONFIRMED",label:"BOS confirmed; no clean POI identified",direction:sweep.direction,maturity:60,poiHigh:null,poiLow:null,distancePoiAtr:null,details:{sweepTime:sweep.ts,bosTime:bars[bos].ts}};const pHigh=bars[poi].high,pLow=bars[poi].low,mid=(pHigh+pLow)/2,dist=Math.abs(structurePrice-mid)/a,inside=structurePrice>=pLow&&structurePrice<=pHigh;let touched=false;for(let i=bos+1;i<n-1;i++)if(bars[i].low<=pHigh&&bars[i].high>=pLow){touched=true;break}if(touched&&!inside)return{stage:2,code:"POI_USED",label:"Recent V2 sequence found, but the POI has already been revisited",direction:sweep.direction,maturity:25,poiHigh:pHigh,poiLow:pLow,distancePoiAtr:dist,details:{sweepTime:sweep.ts,bosTime:bars[bos].ts,poiTime:bars[poi].ts,fresh:false}};if(inside)return{stage:8,code:"ENTRY_ZONE_REACHED",label:"Research entry zone reached — not a trade signal",direction:sweep.direction,maturity:90,poiHigh:pHigh,poiLow:pLow,distancePoiAtr:dist,details:{sweepTime:sweep.ts,bosTime:bars[bos].ts,poiTime:bars[poi].ts,fresh:!touched}};if(dist<=.5)return{stage:7,code:"APPROACHING_POI",label:"Fresh POI identified; structure price is approaching the zone",direction:sweep.direction,maturity:80,poiHigh:pHigh,poiLow:pLow,distancePoiAtr:dist,details:{sweepTime:sweep.ts,bosTime:bars[bos].ts,poiTime:bars[poi].ts,fresh:!touched}};return{stage:6,code:"FRESH_POI_IDENTIFIED",label:"BOS confirmed and a fresh POI is identified",direction:sweep.direction,maturity:70,poiHigh:pHigh,poiLow:pLow,distancePoiAtr:dist,details:{sweepTime:sweep.ts,bosTime:bars[bos].ts,poiTime:bars[poi].ts,fresh:!touched}}}
function regime(h4:Bar[],m15:Bar[],h4t:any){if(h4.length<55||m15.length<55)return"insufficient";const h4atr=rollingMean(tr(h4),14).at(-1)??0,sep=Math.abs((h4t.ema20??0)-(h4t.ema50??0))/Math.max(h4atr,1e-9),ma=rollingMean(tr(m15),14),current=ma.at(-1)??0,base=median(ma.slice(-50));if(current>base*1.45)return"volatility expansion";if(current<base*.68)return"volatility compression";if(sep>.7&&h4t.label!=="mixed")return"trending";if(sep<.22)return"ranging";return"transition"}
function summaryText(symbol:string,trends:any,reg:string,form:any,sess:string,proxy:boolean){const align=trends.d1.label===trends.h4.label&&trends.d1.label!=="mixed"?`${trends.d1.label} D1/H4 alignment`:`D1 ${trends.d1.label}, H4 ${trends.h4.label}`,pull=trends.m15.label!==trends.h4.label&&trends.m15.label!=="mixed"?`; M15 is moving against the H4 bias`:`; M15 is ${trends.m15.label}`,p=proxy?" The structure feed is a context proxy, not broker execution truth.":"";return`${symbol}: ${align}${pull}. Regime: ${reg}. ${form.label}. Active session: ${sess}.${p}`}
async function persistBars(symbol:string,tf:string,bars:Bar[],source:string,isProxy:boolean){const q=await db.from("market_bars").select("*",{count:"exact",head:true}).eq("symbol",symbol).eq("timeframe",tf),initial=(q.count??0)===0,keep=initial?(tf==="15m"?250:tf==="1d"?180:120):5,rows=bars.slice(-keep).map(b=>({symbol,timeframe:tf,ts:b.ts,open:b.open,high:b.high,low:b.low,close:b.close,volume:b.volume,source,is_proxy:isProxy}));if(rows.length){const w=await db.from("market_bars").upsert(rows,{onConflict:"symbol,timeframe,ts,source"});if(w.error)throw new Error(`bars ${symbol}/${tf}: ${w.error.message}`)}}

async function refresh(symbol:string){const cfg=INSTRUMENTS[symbol];if(!cfg)throw new Error("unknown symbol");const[m15r,h1r,d1r,fx,gold]=await Promise.all([yahoo(cfg.chart,"15m","1mo"),yahoo(cfg.chart,"1h","3mo"),yahoo(cfg.chart,"1d","1y"),symbol==="EURUSD"||symbol==="GBPUSD"?fxReference():Promise.resolve(null),symbol==="XAUUSD"?goldReference():Promise.resolve(null)]),m15=cleanCompleted(m15r.bars,TF_SECONDS["15m"]),h1=cleanCompleted(h1r.bars,TF_SECONDS["1h"]),d1=cleanCompleted(d1r.bars,TF_SECONDS["1d"]),h4=cleanCompleted(aggregate(h1,4),4*3600);if(m15.length<60||h1.length<60||d1.length<60)throw new Error("insufficient chart bars");const structurePrice=finite(m15r.meta?.regularMarketPrice)??m15.at(-1)!.close;let referencePrice=structurePrice,bid:number|null=null,ask:number|null=null,quoteSource="Yahoo delayed/reference price";if(symbol==="EURUSD"&&fx?.payload?.rates?.EUR){referencePrice=1/Number(fx.payload.rates.EUR);quoteSource="exchangerate.dev live reference"}if(symbol==="GBPUSD"&&fx?.payload?.rates?.GBP){referencePrice=1/Number(fx.payload.rates.GBP);quoteSource="exchangerate.dev live reference"}if(symbol==="XAUUSD"&&gold?.payload){const gp=gold.payload,p=finite(gp.price??gp?.symbols?.[0]?.price),b=finite(gp.bid??gp?.symbols?.[0]?.bid),a=finite(gp.ask??gp?.symbols?.[0]?.ask);if(p!=null){referencePrice=p;quoteSource="goldprice.dev spot reference"}if(b!=null)bid=b;if(a!=null)ask=a}const trends={d1:trend(d1),h4:trend(h4),h1:trend(h1),m15:trend(m15)},reg=regime(h4,m15,trends.h4),form=formation(m15,structurePrice),sess=session(),atr15=rollingMean(tr(m15),14).at(-1)??null,recent=m15.slice(-20),hi=Math.max(...recent.map(x=>x.high)),lo=Math.min(...recent.map(x=>x.low)),pos=(structurePrice-lo)/Math.max(hi-lo,1e-9),prev=d1.at(-1)!,sw=swings(m15.slice(-80)),warning=cfg.proxy?(symbol==="US30"?"Structure uses E-mini Dow futures as a free extended-hours proxy; not broker US30 CFD execution":"Structure uses COMEX gold futures as a free proxy. Spot XAU reference is displayed separately and is not mixed into futures structure calculations."):"Public market reference. No broker bid/ask execution feed is connected.",health={chart_status:"ok",chart_symbol:cfg.chart,chart_proxy:cfg.proxy,last_m15_bar:m15.at(-1)?.ts,structure_reference_price:structurePrice,structure_source:`${m15r.source} · ${cfg.chart}`,reference_source:quoteSource,fx_reference_status:fx?.status??null,fx_reference_fetched_at:fx?.fetchedAt??null,gold_spot_status:gold?.status??null,gold_spot_fetched_at:gold?.fetchedAt??null,generated_at:new Date().toISOString(),warning},state:any={symbol,as_of:new Date().toISOString(),reference_price:referencePrice,bid,ask,spread:bid!=null&&ask!=null?ask-bid:null,quote_source:quoteSource,chart_source:m15r.source,market_session:sess,d1_trend:trends.d1.label,h4_trend:trends.h4.label,h1_trend:trends.h1.label,m15_trend:trends.m15.label,d1_strength:trends.d1.strength,h4_strength:trends.h4.strength,h1_strength:trends.h1.strength,m15_strength:trends.m15.strength,regime:reg,formation_stage:form.stage,formation_code:form.code,formation_label:form.label,formation_direction:form.direction,formation_confidence:form.maturity,atr15,range_position:clamp(pos,0,1),prev_day_high:prev.high,prev_day_low:prev.low,swing_high:sw.lastHigh?.price??null,swing_low:sw.lastLow?.price??null,poi_high:form.poiHigh,poi_low:form.poiLow,distance_to_poi_atr:form.distancePoiAtr,research_summary:summaryText(symbol,trends,reg,form,sess,cfg.proxy),data_health:health,details:{trends,formation:form.details,chart_name:cfg.name,proxy:cfg.proxy,structure_reference_price:structurePrice},updated_at:new Date().toISOString()};const w=await db.from("market_states").upsert(state,{onConflict:"symbol"});if(w.error)throw new Error(`state write: ${w.error.message}`);await Promise.all([persistBars(symbol,"15m",m15,m15r.source,cfg.proxy),persistBars(symbol,"1h",h1,h1r.source,cfg.proxy),persistBars(symbol,"4h",h4,h1r.source,cfg.proxy),persistBars(symbol,"1d",d1,d1r.source,cfg.proxy)]);const last=await db.from("market_state_history").select("as_of").eq("symbol",symbol).order("as_of",{ascending:false}).limit(1).maybeSingle();if(!last.data||Date.now()-new Date(last.data.as_of).getTime()>=14*60_000){const h=await db.from("market_state_history").insert({symbol,as_of:state.as_of,reference_price:referencePrice,formation_stage:form.stage,formation_code:form.code,formation_direction:form.direction,regime:reg,state:{trends,formation:form,session:sess,quoteSource,structurePrice,spread:state.spread,rangePosition:state.range_position,dataHealth:health}});if(h.error)throw new Error(`history write: ${h.error.message}`)}return state}
async function getState(symbol:string,force=false){const q=await db.from("market_states").select("*").eq("symbol",symbol).maybeSingle();if(!force&&q.data&&Date.now()-new Date(q.data.updated_at).getTime()<90_000)return{...q.data,cached:true};try{return{...await refresh(symbol),cached:false}}catch(e){if(q.data)return{...q.data,cached:true,refresh_error:String(e)};throw e}}
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:CORS});try{const u=new URL(req.url);let symbols:string[]=[],force=false;if(req.method==="POST"){const b=await req.json().catch(()=>({})),raw=b.symbols??b.symbol??"all";symbols=raw==="all"?Object.keys(INSTRUMENTS):(Array.isArray(raw)?raw:[raw]);force=Boolean(b.force)}else{const raw=u.searchParams.get("symbol")??"all";symbols=raw==="all"?Object.keys(INSTRUMENTS):raw.split(",");force=u.searchParams.get("force")==="1"}symbols=symbols.map(x=>String(x).toUpperCase()).filter(x=>INSTRUMENTS[x]);if(!symbols.length)return response({error:"No valid instruments"},400);const settled=await Promise.allSettled(symbols.map(s=>getState(s,force))),states:any[]=[],errors:any[]=[];settled.forEach((r,i)=>r.status==="fulfilled"?states.push(r.value):errors.push({symbol:symbols[i],error:String(r.reason)}));return response({version:"V2 Research Lab v1.2",research_only:true,live_signals:false,generated_at:new Date().toISOString(),states,errors,methodology:{purpose:"Market-state and V2 formation research; not trade execution",formation_scale:"0-8 deterministic state machine",execution_truth:"Unavailable without broker feed",free_data_policy:"No paid API keys; public/no-key reference sources only",price_separation:"External reference prices never alter a different provider's structure geometry",visual_chart:"TradingView widget is separate from research calculations"}})}catch(e){return response({error:String(e)},500)}});
